# Specification: Interactive CLI Mode

## Goal
Allow users to use the tool directly in the terminal without an MCP host.

## Functional Requirements
- Add an `--interactive` (or `-i`) flag.
- Provide a simple command-loop interface.
- Support search and formatting directly in the terminal.
