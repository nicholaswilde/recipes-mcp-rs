# Specification: Advanced Image and Gallery Extraction

## Goal
Improve visual data by finding high-resolution images and process galleries.

## Functional Requirements
- Implement logic to find the largest/highest-res image on a page.
- Extract "step-by-step" or gallery images if available.
- Store additional images in a `gallery` field in the `Recipe` struct.
